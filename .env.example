# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=my_database

# Public site shared credentials
AUTH_USERNAME=user
AUTH_PASSWORD=changeme

# Admin panel credentials (hash generated with password_hash() / bcrypt 10 rounds)
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2y$10$...generated_hash_here...

# Visitor tracking salt (use any random string)
VISITOR_SALT=random_salt_here
