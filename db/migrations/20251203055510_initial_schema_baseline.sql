-- migrate:up
-- Baseline migration: marks the starting point of version control
-- The existing schema is already in place, no SQL needed here
-- Future migrations will add changes from this point forward

-- migrate:down
-- Cannot rollback baseline

